# Anthropic API Key - get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=your_api_key_here

# Server port (default: 3001)
PORT=3001

# App URL (used for OAuth callbacks)
APP_URL=http://localhost:3001

# Database path (default: ./data/body-double.db)
# DATABASE_PATH=./data/body-double.db

# ============================================
# Notion Integration (Optional)
# ============================================
# Get credentials at: https://www.notion.so/my-integrations
# 1. Create a new integration
# 2. Copy the Internal Integration Token (this is your client secret)
# 3. For OAuth, you need to set up a public integration
NOTION_CLIENT_ID=your_notion_client_id
NOTION_CLIENT_SECRET=your_notion_client_secret
NOTION_REDIRECT_URI=http://localhost:3001/api/notion/callback

# ============================================
# Google Integration (Optional)
# ============================================
# Get credentials at: https://console.cloud.google.com/
#
# Setup steps:
# 1. Create a new project (or select existing)
# 2. Enable APIs:
#    - APIs & Services > Library > "Gmail API" > Enable
#    - APIs & Services > Library > "Google Calendar API" > Enable
# 3. Configure OAuth consent screen:
#    - APIs & Services > OAuth consent screen
#    - User Type: External
#    - Fill in app info
#    - Add scopes: gmail.readonly, calendar.readonly
#    - Add test users (your email)
# 4. Create OAuth credentials:
#    - APIs & Services > Credentials > Create Credentials > OAuth client ID
#    - Application type: Web application
#    - Add redirect URI: http://localhost:3001/api/google/callback
#    - Copy Client ID and Client Secret
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3001/api/google/callback
