# Body Double - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED
# =============================================================================

# Anthropic API Key - get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=your_api_key_here

# =============================================================================
# OPTIONAL - Server Configuration
# =============================================================================

# Server port (default: 3001)
PORT=3001

# Node environment: "development" or "production"
# NODE_ENV=development

# =============================================================================
# OPTIONAL - Database Configuration
# =============================================================================

# SQLite database file path (default: ./data/body-double.db)
# For Render.com with persistent disk, use: /var/data/body-double.db
# DATABASE_PATH=./data/body-double.db

# =============================================================================
# OPTIONAL - URL Configuration (for magic link emails)
# =============================================================================

# Base URL for the application (used for magic link generation)
# If not set, falls back to RENDER_EXTERNAL_URL, then localhost:3000
# APP_URL=https://your-domain.com

# Render.com external URL (automatically set by Render)
# RENDER_EXTERNAL_URL=https://your-app.onrender.com
